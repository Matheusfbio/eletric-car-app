// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    id 'com.android.application' version '7.4.2' apply false // Atualizado de 7.2.1
    id 'com.android.library' version '7.4.2' apply false      // Atualizado de 7.2.1
    id 'org.jetbrains.kotlin.android' version '1.7.0' apply false // Considere atualizar o Kotlin também
}


task clean(type: Delete) {
    delete rootProject.buildDir
}